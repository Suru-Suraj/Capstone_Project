name: 'Terraform'
on:
  push:
    branches: [ "main" ]
  pull_request:
permissions:
  contents: read
jobs:
  terraform:
    name: 'Terraform'
    runs-on: ubuntu-latest
    environment: production
    defaults:
      run:
        shell: bash
    steps:
    - name: Checkout
      uses: actions/checkout@v3
    - name: Setup Terraform
      uses: hashicorp/setup-terraform@v2
    - name: Configure AWS Credentials
      uses: aws-actions/configure-aws-credentials@v3
      with:
        aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
        aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        aws-region: us-east-1
    - name: ls
      run: ls
    - name: Change the Directory
      run: cd Terraform_and_Ansible
    - name: ls
      run: ls
    - name: Terraform Init
      run: terraform init -chdir Terraform_and_Ansible
    - name: Terraform Plan
      run: terraform plan -chdir Terraform_and_Ansible
    - name: Terraform Apply
      run: terraform apply -chdir Terraform_and_Ansible -input=false -auto-approve 
    - name: Terraform Destroy
      run: terraform destroy -auto-approve
      if: failure() || success()
    - name: Terraform Output(private)
      run: terraform output private_private_ip
    - name: Terraform Output(public)-private
      run: terraform output public_private_ip
    - name: Terraform Output(public)-public
      run: terraform output public_public_ip
